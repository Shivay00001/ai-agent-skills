# 🔀 Flow Diagrams — [PROJECT_NAME]

> Auto-generated flow documentation  
> **Last Updated:** [DATE]  
> **Tech Stack:** [STACK]  
> **Total Diagrams:** [COUNT]  
> **Generated by:** codebase-flow-diagrams skill

---

## Table of Contents

- [System Architecture Overview](#system-architecture-overview)
- [Directory & Module Structure](#directory--module-structure)
- [Component Dependencies](#component-dependencies)
- [Request/Response Flows](#requestresponse-flows)
- [Data Flows](#data-flows)
- [State Machines](#state-machines)
- [Entity Relationships](#entity-relationships)
- [Authentication & Authorization](#authentication--authorization)
- [Error Handling](#error-handling)
- [Deployment & Infrastructure](#deployment--infrastructure)
- [Event / Message Flows](#event--message-flows)
- [Feature Flows](#feature-flows)
- [Class Hierarchies](#class-hierarchies)
- [CI/CD Pipeline](#cicd-pipeline)
- [Third-Party Integrations](#third-party-integrations)
- [Glossary](#glossary)
- [Legend](#legend)
- [Changelog](#changelog)

---

## System Architecture Overview

<!-- 
  Describe the system at the highest level.
  Show all major subsystems and how they connect.
-->

[2-4 sentence explanation of what this diagram shows]

```mermaid
graph TB
    subgraph "Client Layer"
        Web["🌐 Web App"]
        Mobile["📱 Mobile App"]
    end

    subgraph "API Layer"
        Gateway["🔒 API Gateway"]
        Auth["🛡️ Auth Service"]
    end

    subgraph "Business Layer"
        ServiceA["Service A"]
        ServiceB["Service B"]
    end

    subgraph "Data Layer"
        DB[("🗄️ Database")]
        Cache[("⚡ Cache")]
        Queue["⏱️ Message Queue"]
    end

    subgraph "External"
        ExtAPI["📡 Third-Party API"]
    end

    Web --> Gateway
    Mobile --> Gateway
    Gateway --> Auth
    Gateway --> ServiceA
    Gateway --> ServiceB
    ServiceA --> DB
    ServiceA --> Cache
    ServiceB --> DB
    ServiceB --> Queue
    Queue --> ServiceA
    ServiceA --> ExtAPI
```

**Key Files:**
- [entry-point](file:///path/to/entry)
- [config](file:///path/to/config)

---

## Directory & Module Structure

<!-- Map the project's organizational structure -->

[Explanation of how the project is organized]

```
project-root/
├── src/                    # Source code
│   ├── components/         # UI Components
│   ├── services/           # Business logic
│   ├── models/             # Data models / entities
│   ├── routes/             # API route definitions
│   ├── middleware/          # Request middleware
│   ├── utils/              # Shared utilities
│   └── config/             # Configuration
├── tests/                  # Test suites
├── docs/                   # Documentation
├── scripts/                # Build/deploy scripts
└── infra/                  # Infrastructure-as-code
```

---

## Component Dependencies

<!-- Show which modules depend on which -->

[Explanation of dependency direction and coupling]

```mermaid
graph LR
    Routes["Routes"] --> Controllers["Controllers"]
    Controllers --> Services["Services"]
    Services --> Repositories["Repositories"]
    Repositories --> Database[("Database")]
    Services --> ExternalAPIs["External APIs"]
    Controllers --> Middleware["Middleware"]
    Middleware --> Auth["Auth"]

    style Routes fill:#4A90D9,color:#fff
    style Controllers fill:#4A90D9,color:#fff
    style Services fill:#50C878,color:#fff
    style Repositories fill:#50C878,color:#fff
    style Database fill:#F5A623,color:#fff
    style ExternalAPIs fill:#9B59B6,color:#fff
    style Middleware fill:#4A90D9,color:#fff
    style Auth fill:#E74C3C,color:#fff
```

**Key Files:**
- [dependency-config](file:///path/to/deps)

---

## Request/Response Flows

### [Feature Name] — [HTTP Method] [Endpoint]

[Explain what this flow does and when it triggers]

```mermaid
sequenceDiagram
    participant Client
    participant Gateway as API Gateway
    participant Auth as 🔒 Auth
    participant Controller
    participant Service
    participant DB as 🗄️ Database
    participant Cache as ⚡ Cache

    Client->>Gateway: Request
    Gateway->>Auth: Validate Token
    Auth-->>Gateway: ✅ Valid
    Gateway->>Controller: Route to handler
    Controller->>Service: Business logic
    Service->>Cache: Check cache
    alt Cache Hit
        Cache-->>Service: Cached data
    else Cache Miss
        Service->>DB: Query
        DB-->>Service: Result
        Service->>Cache: Store in cache
    end
    Service-->>Controller: Processed result
    Controller-->>Gateway: Response
    Gateway-->>Client: HTTP Response
```

**Key Files:**
- [route-definition](file:///path/to/route)
- [controller](file:///path/to/controller)
- [service](file:///path/to/service)

---

## Data Flows

<!-- Show how data moves through the system -->

[Explanation of data lifecycle]

```mermaid
flowchart LR
    Input["📥 User Input"]
    Validate["✅ Validation"]
    Transform["🔄 Transform"]
    Store["🗄️ Persist"]
    Index["🔍 Index"]
    Cache["⚡ Cache"]
    Serve["📤 API Response"]
    Render["🖥️ UI Render"]

    Input --> Validate
    Validate -->|Valid| Transform
    Validate -->|Invalid| Input
    Transform --> Store
    Store --> Index
    Store --> Cache
    Cache --> Serve
    Serve --> Render
```

---

## State Machines

### [Entity Name] Lifecycle

[Explain the states and transitions for this entity]

```mermaid
stateDiagram-v2
    [*] --> Created: Initialize
    Created --> Active: Activate
    Created --> Cancelled: Cancel
    Active --> Paused: Pause
    Paused --> Active: Resume
    Active --> Completed: Complete
    Active --> Failed: Error
    Failed --> Active: Retry
    Completed --> Archived: Archive
    Cancelled --> [*]
    Archived --> [*]
```

**Key Files:**
- [state-machine](file:///path/to/state-machine)

---

## Entity Relationships

<!-- Map database entities and their relationships -->

[Explanation of data model]

```mermaid
erDiagram
    USER {
        string id PK
        string email
        string name
        datetime created_at
    }
    ROLE {
        string id PK
        string name
    }
    USER ||--o{ USER_ROLE : has
    ROLE ||--o{ USER_ROLE : assigned_to
    USER_ROLE {
        string user_id FK
        string role_id FK
    }
```

---

## Authentication & Authorization

[Explanation of the auth architecture]

```mermaid
flowchart TD
    Request["Incoming Request"]
    HasToken{"🔒 Has Token?"}
    ValidToken{"✅ Token Valid?"}
    Expired{"⏰ Expired?"}
    RefreshToken["🔄 Refresh Token"]
    CheckPerms{"🛡️ Has Permission?"}
    Login["➡️ Redirect to Login"]
    Grant["✅ Access Granted"]
    Deny["❌ 403 Forbidden"]
    Unauthorized["❌ 401 Unauthorized"]

    Request --> HasToken
    HasToken -->|No| Login
    HasToken -->|Yes| ValidToken
    ValidToken -->|No| Expired
    Expired -->|Yes| RefreshToken
    Expired -->|No| Unauthorized
    RefreshToken -->|Success| CheckPerms
    RefreshToken -->|Fail| Login
    ValidToken -->|Yes| CheckPerms
    CheckPerms -->|Yes| Grant
    CheckPerms -->|No| Deny

    style Grant fill:#27AE60,color:#fff
    style Deny fill:#E74C3C,color:#fff
    style Unauthorized fill:#E74C3C,color:#fff
    style Login fill:#F39C12,color:#fff
```

---

## Error Handling

[Explanation of error handling strategy]

```mermaid
flowchart TD
    Op["Operation"]
    Err{"⚠️ Error?"}
    Success["✅ Return Result"]
    Type{"Error Type"}
    Retry["🔄 Retry w/ Backoff"]
    MaxRetry{"Max Retries?"}
    UserErr["📋 400 Bad Request"]
    SysErr["🚨 Log + Alert"]
    Fallback["🔄 Fallback/Default"]
    CircuitBreaker["⛔ Circuit Open"]

    Op --> Err
    Err -->|No| Success
    Err -->|Yes| Type
    Type -->|Transient| Retry
    Retry --> MaxRetry
    MaxRetry -->|No| Op
    MaxRetry -->|Yes| CircuitBreaker
    Type -->|Validation| UserErr
    Type -->|System| SysErr
    SysErr --> Fallback

    style Success fill:#27AE60,color:#fff
    style UserErr fill:#F39C12,color:#fff
    style SysErr fill:#E74C3C,color:#fff
    style CircuitBreaker fill:#E74C3C,color:#fff
```

---

## Deployment & Infrastructure

[Explanation of deployment architecture]

```mermaid
graph TB
    subgraph "CI/CD Pipeline"
        Commit["📝 Git Commit"]
        CI["🔧 CI Build"]
        Test["🧪 Tests"]
        Scan["🛡️ Security Scan"]
        Build["📦 Build Artifact"]
        Deploy["🚀 Deploy"]
    end

    subgraph "Production"
        LB["⚖️ Load Balancer"]
        App1["🖥️ Instance 1"]
        App2["🖥️ Instance 2"]
        DB[("🗄️ Primary DB")]
        Replica[("📋 Read Replica")]
        Cache[("⚡ Cache")]
        CDN["🌐 CDN"]
    end

    Commit --> CI --> Test --> Scan --> Build --> Deploy
    Deploy --> App1
    Deploy --> App2
    LB --> App1
    LB --> App2
    App1 --> DB
    App2 --> DB
    DB --> Replica
    App1 --> Cache
    App2 --> Cache
    CDN --> LB
```

---

## Event / Message Flows

[Explanation of async/event-driven patterns]

```mermaid
flowchart LR
    Producer["📤 Producer Service"]
    Queue["📬 Message Queue"]
    DLQ["💀 Dead Letter Queue"]
    Consumer1["📥 Consumer A"]
    Consumer2["📥 Consumer B"]
    Retry["🔄 Retry Handler"]

    Producer -->|Publish| Queue
    Queue -->|Subscribe| Consumer1
    Queue -->|Subscribe| Consumer2
    Consumer1 -->|Failed| DLQ
    DLQ --> Retry
    Retry -->|Requeue| Queue
```

---

## Feature Flows

### [Feature Name]

[Explain the complete user journey for this feature]

```mermaid
flowchart TD
    Start["👤 User initiates action"]
    Input["📝 Fill form / Provide input"]
    ClientValidate["✅ Client-side validation"]
    ShowErrors["⚠️ Show validation errors"]
    Submit["📡 Submit to API"]
    ServerValidate["✅ Server-side validation"]
    Process["⚙️ Process business logic"]
    Store["🗄️ Store result"]
    Notify["📧 Send notifications"]
    Response["✅ Success response"]
    UpdateUI["🖥️ Update UI"]

    Start --> Input
    Input --> ClientValidate
    ClientValidate -->|Invalid| ShowErrors --> Input
    ClientValidate -->|Valid| Submit
    Submit --> ServerValidate
    ServerValidate -->|Invalid| ShowErrors
    ServerValidate -->|Valid| Process
    Process --> Store
    Store --> Notify
    Notify --> Response
    Response --> UpdateUI
```

---

## Class Hierarchies

[Explanation of class/interface structure]

```mermaid
classDiagram
    class BaseEntity {
        +String id
        +DateTime createdAt
        +DateTime updatedAt
    }
    class BaseService {
        +validate()
        +execute()
        +handleError()
    }
    class BaseRepository {
        +findById()
        +findAll()
        +save()
        +delete()
    }

    BaseEntity <|-- ConcreteEntity
    BaseService <|-- ConcreteService
    BaseRepository <|-- ConcreteRepository
    ConcreteService --> ConcreteRepository : uses
    ConcreteRepository --> ConcreteEntity : manages
```

---

## CI/CD Pipeline

[Explanation of build and deployment pipeline]

```mermaid
flowchart LR
    subgraph "Build"
        Commit["Commit"] --> Lint["Lint"]
        Lint --> UnitTest["Unit Tests"]
        UnitTest --> IntTest["Integration Tests"]
    end
    subgraph "Quality"
        IntTest --> Coverage["Coverage Check"]
        Coverage --> SecScan["Security Scan"]
        SecScan --> BuildArtifact["Build"]
    end
    subgraph "Deploy"
        BuildArtifact --> Staging["Deploy Staging"]
        Staging --> SmokeTest["Smoke Tests"]
        SmokeTest --> Approval{"Manual Approval"}
        Approval -->|Approved| Prod["Deploy Production"]
        Approval -->|Rejected| Fix["Fix & Retry"]
    end

    style Prod fill:#27AE60,color:#fff
    style Fix fill:#E74C3C,color:#fff
```

---

## Third-Party Integrations

[Explanation of external service dependencies]

```mermaid
graph LR
    App["🏠 Application"]

    App -->|Payments| Stripe["💳 Stripe"]
    App -->|Email| SendGrid["📧 SendGrid"]
    App -->|Storage| S3["📦 AWS S3"]
    App -->|Auth| OAuth["🔐 OAuth Provider"]
    App -->|Monitoring| Datadog["📊 Monitoring"]
    App -->|Logging| CloudWatch["📋 Log Service"]

    style Stripe fill:#6772E5,color:#fff
    style SendGrid fill:#1A82E2,color:#fff
    style S3 fill:#FF9900,color:#fff
    style OAuth fill:#E74C3C,color:#fff
    style Datadog fill:#632CA6,color:#fff
    style CloudWatch fill:#FF4F8B,color:#fff
```

---

## Glossary

| Term | Definition |
|------|-----------|
| [TERM] | [DEFINITION] |

---

## Legend

| Element | Meaning |
|---------|---------|
| 🔒 | Auth-protected endpoint or service |
| ⚡ | Performance-critical / cached |
| 🛡️ | Security boundary |
| 📦 | External package or service |
| 🗄️ | Database operation |
| 📡 | Network / HTTP call |
| ⏱️ | Async / queued operation |
| 🔄 | Retry logic |
| ⚠️ | Error / warning path |
| Blue nodes | Frontend / Client layer |
| Green nodes | Backend / Business layer |
| Orange nodes | Data / Storage layer |
| Purple nodes | External / Third-party |
| Red nodes | Security / Error paths |

---

## Changelog

| Date | Section | Change | Reason |
|------|---------|--------|--------|
| [DATE] | All | Initial generation | First documentation |
